{
  "outpost": ">=1.5.0",
  "submodules": [
    "logstash@1.4.5"
  ],
  "scripts": {
    "install": "install.js",
    "configure": "configure.js",
    "start": "start.js",
    "stop": "stop.js"
  },
  "schema": {
    "configure": {
      "_controller" : {
        "apiUrl": {"type": "string", "title": "API Url", "default": "https://approvalsdev.capriza.com/v1", "required": true},
        "caprizaCreds": {"type": "string", "title": "Capriza Credentials File", "default": "/etc/capriza/creds", "required": true}
      }
    }
  }
}
